# ⚙️ تكوين المنفذ - Port Configuration

## 🎯 الهدف
النظام الآن **مرن تماماً** ويمكنه العمل على **أي منفذ** تريده!

---

## 📋 الخطوات السريعة

### 1️⃣ إنشاء ملف الإعدادات

في مجلد `frontend`، أنشئ ملف باسم `.env.local`:

```bash
cd frontend
# على Windows
echo NEXT_PUBLIC_API_URL=http://localhost:3001/api > .env.local

# أو افتح محرر نصوص وأنشئ الملف يدوياً
```

### 2️⃣ اختر المنفذ المناسب

**مثال 1: المنفذ 3001**
```env
NEXT_PUBLIC_API_URL=http://localhost:3001/api
```

**مثال 2: المنفذ 5000** (الافتراضي حالياً)
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

**مثال 3: المنفذ 8080**
```env
NEXT_PUBLIC_API_URL=http://localhost:8080/api
```

### 3️⃣ أعد تشغيل Frontend

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله مرة أخرى
yarn dev
```

---

## ✅ التحقق من النجاح

بعد إعادة التشغيل، افتح المتصفح واضغط F12 للـ Console، يجب أن ترى:

```
🔍 Attempting to fetch admin data from: http://localhost:YOUR_PORT/api/...
💡 Tip: Set NEXT_PUBLIC_API_URL in .env.local to change the API port
```

---

## 🔧 إعداد Backend على منفذ مختلف

إذا أردت تشغيل Backend على منفذ معين:

```bash
cd backend
# عدّل ملف .env
# PORT=3001  (أو أي منفذ تريده)
node server.js
```

---

## 📝 ملخص

✅ **مرن**: يعمل على أي منفذ  
✅ **سهل**: فقط عدّل `.env.local`  
✅ **آمن**: الملف لن يُرفع إلى Git  
✅ **واضح**: رسائل توضيحية في Console  

**القيمة الافتراضية**: `http://localhost:5000/api`
